events {
    worker_connections 1024;
}

http {
    upstream backend {
        server backend:8000;
    }

    upstream landing {
        server frontend-landing:3000;
    }

    upstream citizen {
        server frontend-citizen:3001;
    }

    upstream municipal {
        server frontend-municipal:3002;
    }

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=30r/s;
    limit_req_zone $binary_remote_addr zone=upload_limit:10m rate=5r/s;

    server {
        listen 80;
        server_name localhost ecovision.local;

        # Gzip compression
        gzip on;
        gzip_types text/plain text/css application/json application/javascript;

        # API
        location /api/ {
            limit_req zone=api_limit burst=50 nodelay;
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_read_timeout 60s;
        }

        # API Docs
        location /docs {
            proxy_pass http://backend;
        }

        # Uploaded images
        location /uploads/ {
            limit_req zone=api_limit burst=100 nodelay;
            proxy_pass http://backend;
        }

        # Citizen App
        location /citizen/ {
            rewrite ^/citizen/(.*) /$1 break;
            proxy_pass http://citizen;
            proxy_set_header Host $host;
        }

        # Municipal Dashboard
        location /municipal/ {
            rewrite ^/municipal/(.*) /$1 break;
            proxy_pass http://municipal;
            proxy_set_header Host $host;
        }

        # Landing page (catch-all)
        location / {
            proxy_pass http://landing;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
